<title>Dashboard - inGallery</title>
<%- layout('layout/userBoilerplate') %>

<div id="imgPreview">
  <div class="topBar">
    <div class="controlIcons">
      <ul>
        <div class="first">
          <a href="/user/dashboard"
            ><button><i class="fa-solid fa-circle-arrow-left"></i></button
          ></a>
          <h1><%= img.filename %></h1>
        </div>

        <div class="icons">
          <li>
            <button
              id="noPadding"
              onclick="downloadImage('<%= img.url %>', '<%= img.filename %>')"
            >
              <i class="fa-solid fa-circle-down"></i>
            </button>
          </li>
          <li>
            <a href="/user/dashboard/<%= img._id %>/edit">
              <button><i class="fa-solid fa-pen-to-square"></i></button>
            </a>
          </li>
          <li>
            <a href="/user/dashboard/<%= img._id %>/delete">
              <button><i class="fa-solid fa-trash" id="delete"></i></button>
            </a>
          </li>
        </div>

        <li id="publish">
          <button id="publishBtn" class="popUpBtn">
            <i class="fa-solid fa-cloud-arrow-up"></i>Publish Image
          </button>
          <div class="popUpBox">
            <div class="publishContent" id="popUpContent">
              <img src="<%= img.url %>" alt="<%= img.filename %>" />
              <h3>Filename: <%= img.filename %></h3>
              <h2>Do you wish to publish this image to Web?</h2>
              <div class="btns">
                <a href="/user/dashboard/<%= img._id %>/publish">Yes</a>
                <a href="#" id="closeButton">No</a>
              </div>
            </div>
          </div>
        </li>
      </ul>
    </div>
  </div>
  <div class="imgSection2">
    <div class="header">
      <h3><% %></h3>
    </div>
    <div class="imgPreview">
      <img src="<%= img.url %>" alt="<%= img.filename %>" id="zoomableImage" />
    </div>
    <div class="footer">
      <div>
        <button id="zoomIn">
          <i class="fa-solid fa-magnifying-glass-plus"></i>
        </button>
        <input
          type="range"
          name="zoomInOut"
          id="zoomInOut"
          min="15"
          max="1000"
          value="15"
        />
        <button id="zoomOut">
          <i class="fa-solid fa-magnifying-glass-minus"></i>
        </button>
        <button id="resetBtn"><i class="fa-solid fa-expand"></i></button>
      </div>
    </div>
  </div>
</div>

<script>
  function downloadImage(url, filename) {
    fetch(url)
      .then((response) => response.blob())
      .then((blob) => {
        const link = document.createElement("a");
        link.href = URL.createObjectURL(blob);
        link.download = filename;
        document.body.appendChild(link);
        link.click();
        document.body.removeChild(link);
      })
      .catch((err) => console.error("Error downloading the image:", err));
  }
</script>
